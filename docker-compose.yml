version: '3.8'

services:
  kubernetes-docs:
    build:
      context: .
      args:
        - VITE_OLLAMA_BASE=${VITE_OLLAMA_BASE:-http://192.168.10.18/ollama/v1}
        - VITE_MODEL_NAME=${VITE_MODEL_NAME:-gpt-oss:20b}
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - app-network
    extra_hosts:
      - "192.168.10.32:192.168.10.32"

networks:
  app-network:
    driver: bridge
